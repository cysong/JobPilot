version: 1
name: "job_analyzer"
model: "deepseek-chat"
temperature: 0.1
max_tokens: 2000

instructions: |
  Extract structured information from job descriptions.

  Rules:
  - ONLY extract explicitly mentioned information
  - Do NOT infer or hallucinate
  - Normalize skill names (e.g., "React.js" -> "React")

output_type: "AnalyzedJob"
tools: []
handoffs: []
